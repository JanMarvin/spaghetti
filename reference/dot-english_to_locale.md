# Translate a function name from English to a locale.

Translate a function name from English to a locale.

## Usage

``` r
.english_to_locale(fn_en, locale)
```

## Arguments

- fn_en:

  English function name (any case).

- locale:

  Locale code or NULL (returns fn unchanged).

## Value

Localised name, or fn unchanged if no mapping found.
