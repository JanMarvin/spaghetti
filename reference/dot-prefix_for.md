# Determine the OOXML namespace tier for a function name.

Determine the OOXML namespace tier for a function name.

## Usage

``` r
.prefix_for(fn)
```

## Arguments

- fn:

  Character scalar, function name (any case).

## Value

One of "legacy", "xlfn", or "xlws".
