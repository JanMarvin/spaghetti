# Known-good SHA-256 of the Microsoft Terminology Collection zip. Recorded once; mismatches warn but do not abort, since Microsoft may republish the zip without notice.

Known-good SHA-256 of the Microsoft Terminology Collection zip. Recorded
once; mismatches warn but do not abort, since Microsoft may republish
the zip without notice.

## Usage

``` r
.MTC_EXPECTED_SHA256
```
