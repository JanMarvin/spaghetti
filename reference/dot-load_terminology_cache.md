# Load the cached RDS into .spaghetti_env. Called from .onLoad() and from setup_terminology() after writing the cache. Silent on missing cache.

Load the cached RDS into .spaghetti_env. Called from .onLoad() and from
setup_terminology() after writing the cache. Silent on missing cache.

## Usage

``` r
.load_terminology_cache()
```
