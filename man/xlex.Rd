% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xlex.R
\name{xlex}
\alias{xlex}
\title{Tokenise and display a formula as an ASCII tree}
\usage{
xlex(formula, locale = NULL, print = TRUE)
}
\arguments{
\item{formula}{Character scalar. Formula with or without leading \code{=}.}

\item{locale}{Two-letter locale code or NULL. Used to select the correct
argument separator (\verb{;} for German etc.) and to translate
localised function names in the display label.}

\item{print}{Logical. Print the tree to the console (default TRUE).
Set FALSE to get the data frame silently.}
}
\value{
A data frame with columns \code{depth}, \code{val}, \code{label}, invisibly.
\code{depth} is the nesting level (0 = root, 1 = top-level arguments, …).
Printed as a side-effect when \code{print = TRUE}.
}
\description{
Parses a formula using spaghetti's lexer and renders the token tree in
the style of \code{tidyxl::xlex()}. Nesting follows the call structure: tokens
inside a function's parentheses appear as children of that function node.
}
\details{
The formula is displayed as-is (no OOXML translation). Pass the result of
\code{to_xml()} if you want to inspect the storage form.
}
\examples{
xlex("=SUM(A1:A10)")
xlex("=IF(A1>0, VLOOKUP(A1, B:C, 2, 0), NA())")
xlex("=LAMBDA(x, x * 2)(5)")
xlex("=SUMMEWENNS(C2:C10; A2:A10; \"Berlin\")", locale = "de")
# Inspect OOXML form
xlex(to_xml("=FILTER(A1:A10, B1:B10 > 0)"))
}
