% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{check_formula}
\alias{check_formula}
\title{Check a formula for unknown function names}
\usage{
check_formula(formula, locale = NULL)
}
\arguments{
\item{formula}{Character scalar or vector of formulas.}

\item{locale}{Locale code or NULL. When set, localised names are
translated before checking.}
}
\value{
A data frame with columns \code{formula}, \code{fn}, \code{suggestion}.
\code{fn} is the unknown function name. \code{suggestion} is a comma-separated
string of close matches, or \code{NA} if no suggestion was found.
Returns an empty data frame (invisibly) if no issues are found.
}
\description{
Tokenises a formula and reports any function names that are not in the
function registry, with spelling suggestions for likely typos.
}
\details{
Unlike \code{to_xml()}, this does not translate — it is a pure linting pass.
}
\examples{
check_formula("=SUIM(A1:A10)")          # typo: SUIM -> SUM
check_formula("=VLOKUP(A1,B:B,2,0)")    # typo: VLOKUP -> VLOOKUP
check_formula(c("=SUM(A1)", "=FLITER(A1:A10,B1:B10>0)"))
}
