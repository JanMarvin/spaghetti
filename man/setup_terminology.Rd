% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/setup.R
\name{setup_terminology}
\alias{setup_terminology}
\title{Download and parse the Microsoft Terminology Collection.}
\usage{
setup_terminology(
  expected_sha256 = .MTC_EXPECTED_SHA256,
  force = FALSE,
  workers = max(1L, parallel::detectCores() - 1L),
  quiet = FALSE
)
}
\arguments{
\item{expected_sha256}{SHA-256 hex digest expected for the downloaded
zip. Defaults to the digest of the version of the zip known to this
release of spaghetti. A mismatch produces a warning (not an error),
since Microsoft may republish the file. Pass \code{NULL} to skip the
check entirely.}

\item{force}{If TRUE, re-download even if a cache exists.}

\item{workers}{Number of parallel TBX-parsing workers. Default
detects cores - 1, capped to 8.}

\item{quiet}{If TRUE, suppress progress messages.}
}
\value{
Invisibly, the path to the cached RDS.
}
\description{
Source()s the parser script bundled in \code{inst/extdata/parse_locales.R},
then calls its \code{download_and_parse_tbx()} function to fetch the zip
from Microsoft's public download URL, validate the SHA-256 (if you
supply one), unzip, parse, and write the resulting translation table
to a per-user cache directory.
}
\details{
Subsequent R sessions load the cache automatically on package attach.
}
\section{Dependencies}{

The parser uses the \code{openxlsx2} package (for XML parsing) and
\code{digest} (for SHA-256 verification). Both are declared in
\verb{Suggests:} and installed only if you call this function.
}

\section{Licensing}{

Microsoft has not published an explicit license for the contents of
the Terminology Collection. The data is downloaded directly from
Microsoft; this package does not redistribute it.
}

\examples{
\dontrun{
setup_terminology()
# Skip verification entirely:
setup_terminology(expected_sha256 = NULL)
}
}
\seealso{
\code{\link[=has_terminology]{has_terminology()}}, \code{\link[=clear_terminology]{clear_terminology()}}, \code{\link[=terminology_info]{terminology_info()}}
}
